# Event interface

The implementation packages eligible review events and evidence bundles for a reasoning agent or human reviewer. Recommendations are schema- and policy-validated before execution resumes. The canonical frozen replay package and decision schema are under `data/historical_replay/v1.0/` and `schemas/`. Historical identifiers are preserved; environment-specific paths are represented symbolically in public derivatives.
