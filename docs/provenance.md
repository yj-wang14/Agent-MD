# Provenance

The private immutable baseline preserves original paths and records. This release candidate preserves their hashes and relationships through `release_path_map.csv` and records every staged transformation in `release_transformations.csv`. Authoritative public derivatives are explicitly named and documented. Persistent operational state is not final-result authority; accepted-state manifests and final quality-control records define the scientific result package.
